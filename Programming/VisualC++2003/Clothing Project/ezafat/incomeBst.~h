#include <iostream.h>
//--------------------------------------------------------
#ifndef INCOMEBST_H
#define INCOMEBST_H
//---------------------------------------------------------
class incomeBst
{
        struct treenode
        {
                char date[10];
                int seek;
                treenode *right, *left;
        };
        private:
                treenode *root;
//                void GlC(treenode, treenode&, treenode&); //Grand Left Data
        public:
                incomeBst(){root=NULL;};
                bool insert (char [], int);
//              bool del ();
                int search (char[]);
                ~incomeBst();
};
#endif
