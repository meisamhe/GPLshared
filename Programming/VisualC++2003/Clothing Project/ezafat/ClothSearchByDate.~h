#include <iostream.h>
#include "ClothSearchDateBst.h"
//--------------------------------------------------------
#ifndef CLOTHSELLINGCODEBST_H
#define CLOTHSELLINGCODEBST_H

//---------------------------------------------------------
class goodBst
{
        struct treenode
        {
                long int code;
                int seek;
                treenode *right, *left;
        };
        private:
                treenode *root;
//                void GlC(treenode, treenode&, treenode&); //Grand Left Data
        public:
                goodBst(){root=NULL;};
                bool insert (long int, int);
//              bool del ();
                int search (long int);
                ~goodBst();
};
#endif
 